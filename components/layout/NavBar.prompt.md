One-line: the whole navigation — two 26px uppercase mono words at opposite edges, no logo, no bar, no underline.

```jsx
<NavBar items={[{ label: "About", href: "#about" }, { label: "Directions", href: "#where" }]} />
```
