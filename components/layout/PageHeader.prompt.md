One-line: the page-opening lockup — giant bubble wordmark over a mono tagline / handle line split to the edges.

```jsx
<PageHeader title="Olive" tagline="A lecture series in bars" handle="@oliveseries" />
```
