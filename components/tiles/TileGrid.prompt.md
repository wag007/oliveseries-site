One-line: the wrapping 10px-gap grid that holds every tile on the homepage; tiles flex-grow from a 360px minimum so rows always fill.

```jsx
<TileGrid>
  <ColorTile color="var(--olive-sage)">Charlas</ColorTile>
  <PhotoTile src="assets/photo-crowd.jpg" />
</TileGrid>
```
