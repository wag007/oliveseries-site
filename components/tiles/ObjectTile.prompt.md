One-line: a coloured tile with a cut-out object floating in it and a two-part mono caption pinned to the bottom.

```jsx
<ObjectTile src="assets/cutout-01.png" color="var(--olive-navy)" title="Poster" meta="Mayo 25’" />
```

The second caption is always the dimmer one (0.6 opacity).
