One-line: a flat-fill grid tile carrying a single 30px uppercase word — the category markers in the homepage grid.

```jsx
<ColorTile color="var(--olive-cream)">Zines</ColorTile>
```

Fills come from the tile palette only (cream, sage, clay, fog, blush). Text stays black on light fills.
